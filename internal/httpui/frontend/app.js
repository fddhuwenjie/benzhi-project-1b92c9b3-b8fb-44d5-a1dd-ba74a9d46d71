const $=s=>document.querySelector(s);async function api(url,opt={}){opt.headers={"Content-Type":"application/json","Idempotency-Key":crypto.randomUUID(),...(opt.headers||{})};const r=await fetch(url,opt),d=await r.json();if(!r.ok)throw Error(d.error||"请求失败");return d}function esc(s){return String(s||"").replace(/[&<>\"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;"}[c]))}async function load(){const d=await api("/api/incidents");$("#incidents").innerHTML='<div class="card-grid">'+d.map(i=>'<article><h3>'+esc(i.showcase_id)+' · '+esc(i.metric)+'</h3><p>'+esc(i.artifact_id)+'，读数 '+i.reading+'（'+i.lower+'～'+i.upper+'）</p><p><span class="tag">'+i.risk+'风险</span> <span class="tag">'+i.status+'</span></p><button onclick="detail(\''+i.id+'\')">查看与处置</button></article>').join('')+'</div>'}async function detail(id){const d=await api('/api/incidents/'+id),i=d.incident;$("#detail").innerHTML='<h2>'+esc(i.id)+' 详情</h2><p>'+esc(i.discovery)+'</p><p>状态：'+i.status+'，修订号：'+i.revision+'</p><h3>时间线</h3>'+d.timeline.map(x=>'<div class="timeline"><b>'+esc(x.action)+'</b> '+esc(x.actor)+'<br>'+esc(x.summary)+'</div>').join('');$("#detail").classList.remove('hidden')}$("#incident-form").onsubmit=async e=>{e.preventDefault();const o=Object.fromEntries(new FormData(e.target));["reading","lower","upper"].forEach(k=>o[k]=Number(o[k]));try{await api('/api/incidents',{method:'POST',body:JSON.stringify(o)});e.target.reset();load()}catch(x){alert(x.message)}};load();
